<p style="color: green"><%= notice %></p>

<h1>Attributes</h1>

<div id="attrs">
  <% @attrs.each do |attr| %>
    <p>
      <%= link_to attr.attr_title, attr %>
    </p>
  <% end %>
</div>

<%= link_to "New attribute", new_attr_path %>
