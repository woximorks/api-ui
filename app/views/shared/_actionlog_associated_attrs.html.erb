<h3 style="text-align:center;">ActionLog</h3>
<div id="actionlog_associated_attrs">

    <div class="product_association" id="roster">
    <h5>Roster</h5>
    <%= image_tag("roster.png") %>
    <% @associated_attrs.joins(:product, :endpoint).where(products: { product_title: "Unassociated" }, endpoints: { endpoint_title: "ActionLog" }).each do |associated_attr| %>
    <ul>
        <li><%= associated_attr.attr.attr_title %></li>
        <li><hr></li>
        <li><%= associated_attr.ui_info %>Associated UI information goes here</li>
    </ul>
    <% end %>
    </div>

    <div class="product_association" id="api">
    <h5>API</h5>
    <%= image_tag("API.png") %>
    <% @associated_attrs.joins(:product, :endpoint).where(products: { product_title: "Unassociated" }, endpoints: { endpoint_title: "ActionLog" }).each do |associated_attr| %>
    <ul>
        <li><%= associated_attr.attr.attr_title %></li>
        <li><hr></li>
        <li><%= associated_attr.ui_info %>Associated UI information goes here</li>
    </ul>
    <% end %>
    </div>

    <div class="product_association" id="engage">
    <h5>Engage</h5>
    <%= image_tag("engage.png") %>
    <% @associated_attrs.joins(:product, :endpoint).where(products: { product_title: "Unassociated" }, endpoints: { endpoint_title: "ActionLog" }).each do |associated_attr| %>
    <ul>
        <li><%= associated_attr.attr.attr_title %></li>
        <li><hr></li>
        <li><%= associated_attr.ui_info %>Associated UI information goes here</li>
    </ul>
    <% end %>
    </div>

</div>

